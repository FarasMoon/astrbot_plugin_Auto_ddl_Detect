{
  "detect_mode": {
    "description": "DDL 检测模式",
    "type": "string",
    "default": "regex",
    "hint": "regex: 使用正则表达式匹配；llm: 使用 LLM 判断是否为 DDL 消息",
    "options": ["regex", "llm"]
  },
  "ddl_keywords": {
    "description": "DDL 关键词（正则模式有效），多个用英文逗号分隔",
    "type": "string",
    "default": "截止,截止时间,截止日期,deadline,ddl,交作业"
  },
  "enable_llm_summary": {
    "description": "是否启用 LLM 总结",
    "type": "bool",
    "default": true
  },
  "enable_auto_reply": {
    "description": "检测到 DDL 后是否自动回复",
    "type": "bool",
    "default": true
  }
}
